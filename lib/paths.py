from os.path import dirname, join
import sys, os

pathMap = join(dirname(dirname(os.getcwd())),"maps")
pathConf = join(dirname(dirname(os.getcwd())), "conf")
pathData = join(dirname(dirname(os.getcwd())), "data")